/**
The StackAlign enum describes the stack children's alignment.
*/
declare enum StackAlign {

/**
Indicates stack's children align to the center of stack.
*/
CENTER,

/**
Indicates stack's children align to the end(bottom/right) of stack.
*/
END,

/**
Indicates stack's children align to the start(top/left) of stack.
*/
START,

}
