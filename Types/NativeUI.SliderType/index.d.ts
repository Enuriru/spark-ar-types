/**
The `SliderType` enum describes the Native UI slider types.
*/
declare enum SliderType {

/**
Color type, slider bar would be rendered and sampled according to the color sampler config
*/
COLOR,

/**
Progress type, slider thumb would snap according to step value
*/
PROGRESS,

/**
Scale type, default slider behavior
*/
SCALE,

}
