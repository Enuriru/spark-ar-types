/**
The `TrackingMode` enum describes how a PlaneTracker is tracking an object.
*/
declare enum TrackingMode {

/**
Indicates that a moving object is being tracked.
*/
MOVING_OBJECT,

/**
Indicates that a horizontal plane is being tracked.
*/
PLANE,

}
