/**
The `State` enum describes the state of a GalleryTexture.
*/
declare enum State {

/**
Indicates that the texture was picked and is available now.
*/
AVAILABLE,

/**
Indicates that the texture selection is pending.
*/
PENDING,

}
