/// <reference path="../Reactive.Vec2Signal/index.d.ts" />

/**
The `Head` class represents a head.
*/
declare interface Head {

/**
```
(get) chin: Vec2Signal
(set) (Not Available)
```

Gets the 2D chin position.
*/
chin: Vec2Signal

/**
```
(get) leftEar: Vec2Signal
(set) (Not Available)
```

Gets the 2D left ear position.
*/
leftEar: Vec2Signal

/**
```
(get) leftEye: Vec2Signal
(set) (Not Available)
```

Gets the 2D left eye position.
*/
leftEye: Vec2Signal

/**
```
(get) nose: Vec2Signal
(set) (Not Available)
```

Gets the 2D nose position.
*/
nose: Vec2Signal

/**
```
(get) rightEar: Vec2Signal
(set) (Not Available)
```

Gets the 2D right ear position.
*/
rightEar: Vec2Signal

/**
```
(get) rightEye: Vec2Signal
(set) (Not Available)
```

Gets the 2D right eye position.
*/
rightEye: Vec2Signal

/**
```
(get) top: Vec2Signal
(set) (Not Available)
```

Gets the 2D head top position.
*/
top: Vec2Signal

}
