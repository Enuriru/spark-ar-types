/// <reference path="../Reactive.ScalarSignal/index.d.ts" />


/**
[{"kind":"capability","capability":"facewave","orCapability":""},{"kind":"introducedBy","version":3745275114}]
*/


/**
The `FacewaveModule` class allows you to get visemes from microphone input.
*/
declare interface Facewave extends Module {

/**
```
(get) AA: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing AA viseme.
*/
AA: ScalarSignal

/**
```
(get) CH: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing CH viseme.
*/
CH: ScalarSignal

/**
```
(get) DD: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing DD viseme.
*/
DD: ScalarSignal

/**
```
(get) EE: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing EE viseme.
*/
EE: ScalarSignal

/**
```
(get) FF: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing FF viseme.
*/
FF: ScalarSignal

/**
```
(get) IH: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing IH viseme.
*/
IH: ScalarSignal

/**
```
(get) KK: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing KK viseme.
*/
KK: ScalarSignal

/**
```
(get) NN: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing NN viseme.
*/
NN: ScalarSignal

/**
```
(get) OH: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing OH viseme.
*/
OH: ScalarSignal

/**
```
(get) OU: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing OU viseme.
*/
OU: ScalarSignal

/**
```
(get) PP: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing PP viseme.
*/
PP: ScalarSignal

/**
```
(get) RR: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing RR viseme.
*/
RR: ScalarSignal

/**
```
(get) SIL: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing SIL viseme.
*/
SIL: ScalarSignal

/**
```
(get) SS: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing SS viseme.
*/
SS: ScalarSignal

/**
```
(get) TH: ScalarSignal
(set) (Not Available)
```

Returns a `ScalarSignal` representing TH viseme.
*/
TH: ScalarSignal

}



/**

TODO

*/